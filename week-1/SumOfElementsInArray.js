//declaring an array
let marks=[90,78,65,98];
//initializing with zero to add all elements
let sum=0;
for(let i=0;i<marks.length;i++){
sum+=marks[i]
}
//printing out the sum
console.log(`sum of the elements in the array is ${sum}`);
